<% if @feed_items.any? %>
  <ol class="microposts">

    <!-- The status feed partial defers (by knowing the class)
    the rendering to the micropost partial _micropost -->
    <%= render @feed_items %>


  </ol>
  <%= will_paginate @feed_items %>
<% end %>